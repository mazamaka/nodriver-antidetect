# Core browser automation
nodriver>=0.38

# Cloudflare bypass (optional)
# Install from git if needed:
# git+https://github.com/user/nodriver-cf-verify.git

# Utilities
loguru>=0.7.0
pydantic>=2.0.0
pydantic-settings>=2.0.0

# HTTP client for proxy testing
httpx>=0.27.0
httpx[socks]

# For fingerprint generation
faker>=25.0.0
